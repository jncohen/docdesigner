% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/specimens.R
\name{designer_specimens}
\alias{designer_specimens}
\title{Render a PDF specimen for every installed style}
\usage{
designer_specimens(
  styles = NULL,
  output_dir = "rendered-examples/specimens",
  index = TRUE,
  open = interactive(),
  quiet = TRUE
)
}
\arguments{
\item{styles}{Character vector of style ids. Defaults to every installed
style, as reported by \code{\link[=designer_styles]{designer_styles()}}.}

\item{output_dir}{Directory to write specimens and the index into.}

\item{index}{If \code{TRUE}, write \code{index.html} linking every specimen.}

\item{open}{If \code{TRUE}, open the index in a browser.}

\item{quiet}{Passed to \code{\link[rmarkdown:render]{rmarkdown::render()}}.}
}
\value{
A data frame of results, invisibly: one row per style with its
status, output path, render time, and any error digest.
}
\description{
Renders the bundled specimen document once per style, then writes an HTML
index grouped by style set. Styles are rendered independently: one failure
does not stop the rest, and failures are reported rather than thrown.
}
\details{
Requires a working LaTeX installation (\code{xelatex}) and Pandoc. Run
\code{\link[=designer_check]{designer_check()}} first if you are unsure.
}
