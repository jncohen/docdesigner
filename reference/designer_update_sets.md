# Update installed style sets

Update installed style sets

## Usage

``` r
designer_update_sets(set = NULL)
```

## Arguments

- set:

  Optional set id; defaults to all user sets that record a source.

## Value

Invisibly `NULL`.
